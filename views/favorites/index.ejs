<section class="results-area">
	<h3>FAVORITES</h3>
	<ul>
	<% favList.forEach(function(movie){ %>
		<li>
		<%- link_to(movie.title, "/movies/" + movie.imdbId) %> 
		<%- link_to("add a comment", )%> 
		</li>
	<% }); %>
	</ul>
</section>